- name: variables preference
  hosts: local
  connection: local
  vars:
  - NAME: "Ansible from Play"
  # vars_files:
  # - course.yaml
  # vars_prompt:
  # - name: NAME
  #   prompt: "please enter name"
  #   private: false
  tasks:
  - name: print name
    # vars:
    #   NAME: "Ansible from TASK"
    ansible.builtin.debug:
      msg: "hi {{ NAME }}"  

      # 1.first preference is ars
      # 2. secound preference is task
      # 3. third preference is file
      # 4. forth preference is prompt
      # 5. 
