- name: installing packages
  hosts: local
  connection: local
  tasks:
  - name: instal packages
    ansible.builtin.packages:
       name: "{{ item }}"
       state: present
    loop:
    - mysql
    - nginx
    - zip 
